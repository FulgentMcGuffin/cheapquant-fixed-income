"""Batch bond-analytics: compute analytics for many issuers/dates in parallel.

See ``batch_bond_analytics.py`` (repo root) for the CLI entry point, and
``cqfi.batch.engine.BatchEngine`` for the orchestration used by both the
console (``--no-gui``) and GUI (``cqfi.gui.batch_dialog``) front ends.
"""
